// this version simply imports a clock from its container
// implementation lives in Shared/Components since it's now reused by ArrayOfClocks
export { default as ClockDisplay } from "../Shared/Components/SimpleClockDisplay";
